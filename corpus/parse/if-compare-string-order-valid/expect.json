{
  "description": "IF compare-ops string order is not raw ASCII (and digit strings compare lexicographically when quoted)",
  "parse": {
    "should_parse": true,
    "top_level_statement": "ifStmt"
  },
  "tags": [
    "if",
    "compare"
  ]
}
