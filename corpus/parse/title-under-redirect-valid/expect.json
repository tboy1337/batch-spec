{
  "description": "TITLE parses as a generic command (window title still updates when stdout is redirected at runtime)",
  "parse": {
    "should_parse": true,
    "top_level_statement": "genericCmd",
    "top_level_statements": [
      "genericCmd",
      "genericCmd",
      "genericCmd"
    ]
  },
  "tags": [
    "title",
    "redirection"
  ]
}
