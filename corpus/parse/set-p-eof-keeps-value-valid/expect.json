{
  "description": "SET /P with EOF/NUL input keeps the prior value (parse structure)",
  "parse": {
    "should_parse": true,
    "top_level_statement": "genericCmd"
  },
  "tags": [
    "set",
    "set-p"
  ]
}
