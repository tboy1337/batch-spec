{
  "description": "Parse IF EQU under default extensions ON; at runtime EQU requires extensions (cmd /E:OFF yields 'EQU was unexpected')",
  "parse": {
    "should_parse": true,
    "top_level_statement": "ifStmt"
  },
  "tags": [
    "if",
    "extensions"
  ]
}
