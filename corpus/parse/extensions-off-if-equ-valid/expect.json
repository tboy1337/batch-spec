{
  "description": "Parse IF EQU after setlocal DisableExtensions; at runtime EQU requires extensions",
  "parse": {
    "should_parse": true,
    "top_level_statement": "ifStmt"
  },
  "tags": [
    "if",
    "extensions"
  ]
}
