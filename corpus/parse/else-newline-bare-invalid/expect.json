{
  "description": "ELSE after newline following non-paren THEN is detached (unknown command, not IF else)",
  "parse": {
    "top_level_statement": "detachedElseStmt",
    "should_parse": true
  },
  "tags": [
    "if",
    "else",
    "negative"
  ]
}
