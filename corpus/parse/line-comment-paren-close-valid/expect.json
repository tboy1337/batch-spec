{
  "description": ":: with ) inside paren block historically could early-close; modern Win10 often does not — prefer REM in blocks (parse accepts)",
  "parse": {
    "should_parse": true,
    "top_level_statement": "ifStmt"
  },
  "tags": [
    "if",
    "remarks",
    "negative"
  ]
}
