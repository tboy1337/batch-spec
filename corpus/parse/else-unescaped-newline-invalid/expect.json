{
  "description": "Newline-detached ELSE after IF block: else is unknown command; orphan (block) still runs",
  "parse": {
    "should_parse": true,
    "top_level_statement": "detachedElseStmt"
  },
  "tags": [
    "if",
    "else",
    "negative"
  ]
}
